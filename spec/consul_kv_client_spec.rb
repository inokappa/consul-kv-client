require 'spec_helper'

describe ConsulKvClient do
  it 'has a version number' do
    expect(ConsulKvClient::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
